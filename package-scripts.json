{
  "scripts": {
    "prod-prep": "tsx src/scripts/production-prep.ts",
    "prod-check": "npm run prod-prep && npm run build && npm run test:prod",
    "test:prod": "NODE_ENV=production npm run test",
    "analyze-bundle": "npm run build -- --analyze",
    "clean-logs": "find src -name '*.tsx' -o -name '*.ts' | xargs sed -i 's/console\\.log.*;//g'",
    "deploy-check": "npm run prod-prep && npm run build && echo '✅ Ready for deployment!'"
  }
}